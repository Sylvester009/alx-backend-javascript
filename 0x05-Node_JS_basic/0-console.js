/**
 * show a message on the STDOUT.
 * @param {String} arg  message to be shown.
 * @author sylvester samuel <https://github.com/sylvester009>
 */
const displayMessage = (arg) => {
  console.log(arg);
};

module.exports = displayMessage;
